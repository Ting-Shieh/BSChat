# Kickoff: M1 進場（Email＋密碼註冊／登入）· 2026-07-14

## 1. 模組目的（一句話複習）
讓**一般用戶**自助建立／登入帳號（Email＋密碼＋Google），拿掉產品 Dev 進場；邀請只入團；方案與能力閘延續既有 entitlement。  
對應：PRD v4 §6.5、DDR-v4-8、`BSChat_PM_M1.md` F-1.1～1.8b、US-M1-01～07。

## 2. 在系統架構裡的位置
- 我依賴誰（上游）：既有 User／JWT、Google OAuth、Resend 寄信、`user_entitlements`（試用已落地）、團隊邀請
- 誰依賴我（下游）：全 App 需真實登入；M5／M11 閘門不變，但終端用戶從此不再靠 Dev
- 對外介面：
  - API：`POST /auth/register`、`POST /auth/login`、forgot／reset password；Google 既有；**產品不呼叫** `dev-login`
  - UI：`/register`、`/login`、`/forgot-password`；設定頁移除一鍵切方案（改 CTA）
  - DB：`users.password_hash`（或等價）

## 3. 存在性審視（強制三問 — 不能略過）
- [x] 這個模組現在還該存在嗎？→ **是**（網絡消費端沒進場＝產品長不出來）
- [x] 範圍還對嗎？→ **本次＝進場切片**（密碼註冊／登入／忘記密碼／去 Dev UI）；**不做**金流、SSO、強制驗證阻擋進 App、細 RBAC
- [x] 依賴關係還對嗎？→ **是**；試用／公開池閘已在 M1 試用切片；本切片只補「人怎麼進來」

> **我的判斷**：本切片仍必要。上次 kickoff 只做「公開推薦終身試用」，進場仍靠 Dev——與 PRD v4「一般用戶」矛盾。範圍應獨立為進場切片，不重做 entitlement 計數。與 PM_M1／用戶「要密碼、可拿掉測試進場」一致。

## 4. 自上次工作以來，上游有變動嗎？
- PRD：增 §6.5、DDR-v4-8；認證由 magic 主路徑 → **Email＋密碼＋Google**
- 系統架構：試用 SA/SD 已有；**進場／password 尚無 SA/SD**
- project-context：M1 PM 定稿；密碼決策覆寫；Dev 進場移出產品
- 鄰近模組：M5 Plan／M11 opt-in／試用閘已接；不阻塞本切片，但正式用戶進場後才能真實驗證漏斗
- 上次 M1 kickoff：`m1-public-trial-20260714.md`（試用切片，範圍不同 → **本次完整 kickoff，非 delta**）

## 5. 本次 session 範圍
- 階段：**SA/SD 細節**（通過後才 UIUX → ENG → Code；不跳階段）
- 預計產出：
  1. `spec/architecture/BSChat_SA-SD_M1.md` **增量**（或独立 `…_M1_access.md`）：password 欄位、register／login／reset 契約、與 Google 合併規則、Dev UI 下架邊界
  2. 更新 register／project-context
- 完成標準：可回答「密碼怎存、註冊失敗碼、重設流程、Google＋密碼同 email、前端拿掉哪些 Dev 面」；本階段**不寫業務 code**

## 6. 🚩 紅旗
- **無阻塞開工 SA/SD**。
- 已知：本機／CI 仍可能需要 `ALLOW_DEV_LOGIN` 或測試用 register API——須在 SA/SD 寫清「僅測試，非產品」。
- 提醒：忘記密碼依賴 Resend；本機無 key 時的開發策略（測試專用 endpoint／mail catcher）在 ENG 定，不開回產品 Dev 登入頁。

## 7. 決議
- [x] 通過，開工 SA/SD（用戶 2026-07-14「繼續」）
- [ ] 暫停，回上游：________
