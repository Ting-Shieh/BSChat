# Kickoff: M5 AI 搜尋（Plan UI）· 2026-07-14

## 1. 模組目的（一句話複習）
對話式反向搜尋 → **可見固定五步 Plan** → 分區結果（你的庫／公開推薦）→ 商機簡報。  
對應 PRD v4 §2.1、§5、US-v4-1／2；原型 `spec/screens/screen-4-plan-search.html`。

## 2. 在系統架構裡的位置
- 我依賴誰（上游）：M1 entitlement、M3 聯絡人索引、M6 公司情報、團隊池、（Next）M11 公開池
- 誰依賴我（下游）：商機行動（M8）、Aha UX
- 對外介面：`POST /search/queries`、前端 `SearchPage`、結果 `source_pool`

## 3. 存在性審視（強制三問）
- [x] 這個模組現在還該存在嗎？→ **是**（殺手時刻與 Plan 體感的載體）
- [x] 範圍還對嗎？→ **本次只做 Plan UI + 分區標示強化**；接公開池計數／試用屬 Next（M1+M5b），本次步驟 3 在無權限時「略過」即可
- [x] 依賴關係還對嗎？→ **是**；不改 retrieval 契約，只加前端計劃態與呈現

> 判斷：M5 仍合理；範圍收斂為 Now 的 Plan 包裝，避免一次做完整公開池 entitlement。

## 4. 自上次工作以來，上游有變動嗎？
- PRD：v3→**v4 已確認**（Plan、分區、公開池門檻）
- 系統架構：無新 SA 文件；沿用現有 M5 API
- project-context：網絡方向、wedge 四題拍板、screen-4
- 鄰近模組：團隊池／簡報已落地；M11 語意待 Next

## 5. 本次 session 範圍
- 階段：**Frontend（+ 必要時極小後端）**
- 預計產出：
  1. `SearchPlanPanel`：固定五步，搜尋中串流動畫；無公開權限／試用耗盡時步驟 3 略過
  2. `SearchPage`：結果分「你的名片庫」「AI 推薦 · 公開身份」區塊（取代僅 filter chip）
  3. kickoff／register 更新
- 完成標準：送出問句可見 Plan → 完成後分區＋既有簡報；Pro 有公開結果時進 B 區；非 Pro 步驟 3 標略過

## 6. 🚩 紅旗
無阻塞。已知：Free 終身 2 次計數尚未實作 → 本次以「非 Pro = 略過步驟 3」近似；計數留 M1 re-kickoff。

## 7. 決議
- [x] 通過，開工（用戶「繼續吧」）
